DROP TABLE CLIENTES;

CREATE TABLE CLIENTES (
    CODIGO          INTEGER NOT NULL,
    NOME            VARCHAR(80) NOT NULL,
    PESSOAJURIDICA  VARCHAR(1),
    CNPJCPF         VARCHAR(18),
    ENDERECO        VARCHAR(80),
    BAIRRO          VARCHAR(80),
    CIDADE          VARCHAR(80),
    UF              VARCHAR(2),
    CEP             VARCHAR(8),
    TELEFONE         VARCHAR(20),
    DATANASCIMENTO  DATE,
    SEXO            VARCHAR(1)
);

ALTER TABLE CLIENTES ADD CONSTRAINT PK_CLIENTES PRIMARY KEY (CODIGO);

ALTER TABLE CLIENTES ADD CONSTRAINT UNQ1_CLIENTES UNIQUE (CNPJCPF);

DROP TABLE FORNECEDORES;

CREATE TABLE FORNECEDORES (
    CODIGO           INTEGER NOT NULL,
    RAZAOSOCIAL      VARCHAR(80),
    PESSOAJURIDICA  VARCHAR(1),
    CNPJCPF         VARCHAR(18),
    ENDERECO        VARCHAR(80),
    BAIRRO          VARCHAR(80),
    CIDADE          VARCHAR(80),
    UF              VARCHAR(2),
    CEP             VARCHAR(8),
    TELEFONE         VARCHAR(20),
    LIMITEDECREDITO  NUMERIC(15,2),
    DATAINICIO       DATE
);

ALTER TABLE FORNECEDORES ADD CONSTRAINT PK_FORNECEDORES PRIMARY KEY (CODIGO);

ALTER TABLE FORNECEDORES ADD CONSTRAINT UNQ1_FORNECEDORES UNIQUE (CNPJCPF);

DROP TABLE VENDEDORES;

CREATE TABLE VENDEDORES (
    CODIGO              INTEGER NOT NULL,
    NOME                VARCHAR(80) NOT NULL,
    PESSOAJURIDICA      VARCHAR(1),
    CNPJCPF             VARCHAR(18),
    ENDERECO        VARCHAR(80),
    BAIRRO          VARCHAR(80),
    CIDADE          VARCHAR(80),
    UF              VARCHAR(2),
    CEP             VARCHAR(8),
    TELEFONE         VARCHAR(20),
    PERCENTUALCOMISSAO  NUMERIC(15,2),
    VOLUMEGERALVENDAS   NUMERIC(15,2),
    DATANASCIMENTO      DATE
);

ALTER TABLE VENDEDORES ADD CONSTRAINT PK_VENDEDORES PRIMARY KEY (CODIGO);

ALTER TABLE VENDEDORES ADD CONSTRAINT UNQ1_VENDEDORES UNIQUE (CNPJCPF);

DROP TABLE EMPREGADOS;

CREATE TABLE EMPREGADOS (
    CODIGO              INTEGER NOT NULL,
    NOME                VARCHAR(80) NOT NULL,
    PESSOAJURIDICA      VARCHAR(1),
    CNPJCPF             VARCHAR(18),
    ENDERECO            VARCHAR(80),
    BAIRRO              VARCHAR(80),
    CIDADE              VARCHAR(80),
    UF                  VARCHAR(2),
    CEP                 VARCHAR(8),
    TELEFONE            VARCHAR(20),
    DATANASCIMENTO      DATE,
    NOMEPAI             VARCHAR(80),
    NOMEMAE             VARCHAR(80)
);

ALTER TABLE EMPREGADOS ADD CONSTRAINT PK_EMPREGADOS PRIMARY KEY (CODIGO);

ALTER TABLE EMPREGADOS ADD CONSTRAINT UNQ1_EMPREGADOS UNIQUE (CNPJCPF);

DROP TABLE DEPENDENTES;

CREATE TABLE DEPENDENTES (
    CODIGOEMPREGADO     INTEGER NOT NULL,
    CPF                 VARCHAR(18) NOT NULL,
    NOME                VARCHAR(80) NOT NULL,
    ENDERECO            VARCHAR(80),
    BAIRRO              VARCHAR(80),
    CIDADE              VARCHAR(80),
    UF                  VARCHAR(2),
    CEP                 VARCHAR(8),
    TELEFONE            VARCHAR(20),
    DATANASCIMENTO      DATE,
    NOMEPAI             VARCHAR(80),
    NOMEMAE             VARCHAR(80)
);

ALTER TABLE DEPENDENTES ADD CONSTRAINT PK_DEPENDENTES PRIMARY KEY (CODIGOEMPREGADO, CPF);
